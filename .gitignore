# ===== Unreal Engine generated / build output =====
# These are regenerated by the engine and should never be committed.
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/

# ===== IDE / editor =====
.vs/
.vscode/
.idea/
*.sln
*.slnx
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
*.xcodeproj/
*.xcworkspace/

# ===== OS cruft =====
.DS_Store
Thumbs.db
Desktop.ini

# NOTE: Content/ is intentionally NOT ignored — .uasset/.umap binaries are tracked.
# If this repo grows large, consider migrating Content binaries to Git LFS.

# ===== Oversized plugin ML-model binaries (exceed GitHub's 100 MB hard limit) =====
# MetaHuman Body Tracker ships multi-GB offline model weights (ViTPose 2.4 GB, etc.).
# These are re-downloadable with the plugin and must never be committed.
Plugins/MetaHumanBodyTracker_5.8/Content/
